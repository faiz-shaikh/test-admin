// import { fromJS } from 'immutable';
// import { makeSelectManualFareUploadDomain } from '../selectors';

// const selector = makeSelectManualFareUploadDomain();

describe('makeSelectManualFareUploadDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
